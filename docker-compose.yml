services:
  app:
    build:
      context: .
      target: runtime
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=development
    command: npm run ng serve -- --host 0.0.0.0 --port 4200
    restart: unless-stopped
